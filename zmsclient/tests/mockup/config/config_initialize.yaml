# initialize_scenario.yaml
request:
  method: GET
  path: "/initialize-config-test"
response:
  statusCode: 200
control:
  scenario:
    name: token
    requiredState:
    - not_started
    - configbasic
    - configtoken
    newState: configtoken # Sets the initial state to 'not_started'
